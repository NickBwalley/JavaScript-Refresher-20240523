const number = 1; //primitive type
const num2 = number; // creates a real copy of number
console.log(num2);

// objects and arrays are reference types. If you reassign them you are copying the pointer and ot the value.
const person = {
  name: "Nick",
};

const secondPerson = person;
console.log(secondPerson);
